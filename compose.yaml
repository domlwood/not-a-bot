services:
  postgres:
    image: postgres
    restart: always
    shm_size: 128mb    
    environment:
      POSTGRES_DB: notABot
      POSTGRES_USER: notABot
      POSTGRES_PASSWORD: notABot
    ports:
      - "5432:5432"